[project]
name = "Sign-pipeline"
version = "0.1.0"
requires-python = "~=3.12.0"
dependencies = [
    "numpy",
    "pyyaml",
    "yt_dlp[default]",
    "silero-vad",
    "faster-whisper",
    "torchcodec",
    "python-dotenv",
    "ultralytics",
    "mediapipe",
    "opencv-python"
]